pipeline {
    agent any

    stages {
        stage('Run Python') {
            steps {
                sh 'python3 hello.py'
               bat 'python hello.py'
            }
        }
    }
